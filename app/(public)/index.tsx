import { Text } from "@/components/ui";
import { View } from "react-native";

export default function PublicIndex() {
  return (
    <View className="flex-1 justify-center items-center">
      <Text>Welcome visitor — this is a public page</Text>
    </View>
  );
}
